{
  "_note": "Cron schedules lifted out of vercel.json for the Vercel Hobby test deploy. Hobby allows only 2 cron jobs at once-per-day frequency, and none of these are needed for UI testing. Paste the `crons` array back into vercel.json when moving to a Pro plan, or drive these endpoints from an external scheduler using the CRON_SECRET header.",
  "crons": [
    {
      "path": "/api/cron/release-stock",
      "schedule": "*/5 * * * *"
    },
    {
      "path": "/api/cron/reconcile-orders",
      "schedule": "*/30 * * * *"
    },
    {
      "path": "/api/cron/daily-summary",
      "schedule": "0 8 * * *"
    }
  ]
}
